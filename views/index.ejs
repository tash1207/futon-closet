<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Futon Closet</title>

  <!-- CSS -->
  <!-- load bootstrap and personal stylesheet -->
  <link href="//maxcdn.bootstrapcdn.com/bootswatch/3.2.0/superhero/bootstrap.min.css" rel="stylesheet">
  <link href="css/style.css" rel="stylesheet">

</head>
<body>
  <div class="background">
    <div class="container">
      <div class="jumbotron">
        <h1>Futon Closet</h1>
        <h3>The world's first closet-themed sleeping experience</h3>
      </div>

      <% if (reviews.length > 0) { %>
        <div class="jumbotron">
          <h2>Reviews</h2>
          <ul>
            <% for (const review of reviews) { %>
              <li>
                <p><%= review.text %>
                <% if (review.author) { %>
                  - <%= review.author %>
                <% } %>
                <p>
              </li>
            <% } %>
          </ul>
        </div>
      <% } %>

      <div class="jumbotron">
        <h2>Leave a review!</h2>
        <form method="post" action="/addReview">
          <input type="text" name="reviewAuthor" placeholder="Name" maxLength="30">
          <textarea type="text" name="reviewText" placeholder="Write your review here" cols="40" rows="5" maxlength="300" required></textarea>
          <input type="submit" value="Submit">
        </form>
      </div>

      <div class="jumbotron">
        <h2>Location</h2>
        <p>Futon Closet is located in Sunnyvale, California.<p>
        <p>Conveniently down the street from a VTA stop, guests can take the VTA to the Mountain View Caltrain Station and get to San Francisco in an hour and a half. Or rent a car and be in SF in just 45 minutes.<p>
        <div id="map"></div>
      </div>
    </div>
  </div>
  <script>
  function initMap() {
    const latLng = {lat: 37.40535, lng: -122.01485};
    const map = new google.maps.Map(document.getElementById('map'), {
      zoom: 9,
      center: latLng
    });
    const marker = new google.maps.Marker({
      position: latLng,
      map: map
    });
  }
  </script>
  <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyD42geKjQgyiZl4aPTNd-ph7-V_0OZj8co&callback=initMap">
  </script>
</body>
</html>
